---
stage: building   # idea | spec | building | testing | live
updated: 2026-08-18
---

# STEPS

> این تنها فایلی است که هر سشن عوض می‌شود. کوتاه نگهش دار.
> اگر از یک صفحه بلندتر شد، یعنی دارد تبدیل به آرشیو می‌شود، خلاصه‌اش کن.
> برج مراقبت سه بخش زیر را می‌خواند. تیترها را عوض نکن.

## کجاییم

- ابزار روی لوکال کامل کار می‌کند: `python dashboard/api.py 8080` و بعد menapark.com را بزن. جمع‌آوری شواهد، تحلیل با Gemini (به‌همراه اسکرین‌شات)، گزارش markdown و داشبورد چهارزبانه.
- بزرگ‌ترین اصلاح این سشن: تحلیلگر دیگر ایراد عمومی (H1، schema، alt) را به‌عنوان «شکاف بازاریابی» تحویل نمی‌دهد. آن‌ها رفتند به بخش جدا Hygiene و هر یافته حالا باید به چیزی مشخص روی همان سایت اشاره کند.
- PageSpeed تازه راه افتاد، قبلاً بی‌سروصدا خراب بود چون درخواست بدون کلید سهمیه صفر دارد. کلید حالا در `config/secrets.yaml` است.

## قدم بعدی

- [ ] تب Pain Points را دو تکه کن: «نقاط ضعف سایت» (همین چیز فعلی) و «درد بازار» جدید که هم درد مشتریِ سایت و هم درد خود کسب‌وکار را جدا از هم دربیاورد. عرفان هر دو را خواسته، جدا از هم.
- [ ] دکمه خروجی گزارش به داشبورد اضافه کن (فایل‌های `reports/<domain>.md` و `.json` از قبل ساخته می‌شوند، فقط لینک دانلود لازم است).
- [ ] پیام فیدبک مصطفی را با یافته جدید کامل کن: بج «Level 5 Consulting Group» روی متن تایتل کارت PMP می‌افتد.

## بلاک

- (خالی)

## انجام شده

- [x] تعریف واقعی «شکاف بازاریابی» در پرامپت تحلیلگر + فیلتر قطعی `_split_hygiene` به‌عنوان تور ایمنی
- [x] استخراج متن دیده‌شدنی صفحه، هدینگ‌ها، CTAها، auth providers و بخش‌های خالی (`empty_sections`)
- [x] فرستادن اسکرین‌شات به Gemini، قبلاً گرفته می‌شد ولی هیچ‌وقت به مدل داده نمی‌شد و همین باعث ادعای اشتباه «Clean UI» شده بود
- [x] پشتیبانی از کلید PageSpeed + رفع باگ نمایش امتیاز (۰.۸۵ به‌جای ۸۵)
- [x] تاریخ ثبت دامنه، انقضا، ثبت‌کننده و تماس‌های WHOIS در تب Identity
- [x] تولتیپ توضیحی روی همه کارت‌ها، هر چهار زبان
- [x] پاک شدن نتایج سایت قبلی موقع آنالیز دامنه جدید + autocomplete دامنه‌های قبلی
