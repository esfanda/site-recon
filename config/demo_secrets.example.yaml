# Copy to demo_secrets.yaml on the Pi. Do not commit demo_secrets.yaml.
# Used only when the dashboard is started with --public-demo.
# SITE_RECON_DEMO_KEY in the environment wins if both are set.
gemini_api_key: replace-me
