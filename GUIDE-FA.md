# راهنمای فارسی Site Recon

این ابزار یک آدرس سایت می‌گیرد و به تو می‌گوید آن کسب‌وکار چیست، چطور پول درمی‌آورد، مخاطبش کیست، و کجای مارکتینگش می‌لنگد.

همه چیز روی کامپیوتر خودت اجرا می‌شود. هیچ داده‌ای جایی آپلود نمی‌شود.

---

## قدم اول: پایتون را نصب کن

اگر از قبل نصب داری، از این قدم رد شو.

۱. برو به [python.org/downloads](https://www.python.org/downloads/) و دکمه زرد بزرگ را بزن.

۲. **مهم‌ترین نکته کل این راهنما:** در همان صفحه اول نصب‌کننده، تیک **Add python.exe to PATH** را بزن، بعد Install را بزن.

اگر این تیک را نزنی، ابزار کار نمی‌کند. اکثر آدم‌ها دقیقاً همین‌جا گیر می‌کنند.

---

## قدم دوم: نصب

۱. فایل ZIP را از حالت فشرده خارج کن (راست‌کلیک، Extract All).

۲. وارد پوشه شو و روی **`install.cmd`** دابل‌کلیک کن.

۳. یک پنجره مشکی باز می‌شود و شروع به نصب می‌کند. چند دقیقه طول می‌کشد. دست نزن تا تمام شود.

اگر ویندوز پیام امنیتی داد، روی **More info** و بعد **Run anyway** بزن. این فایل فقط یک اسکریپت نصب ساده است و می‌توانی با Notepad بازش کنی و ببینی داخلش چیست.

---

## قدم سوم: اجرا

روی **`start.cmd`** دابل‌کلیک کن. مرورگر خودش باز می‌شود و داشبورد می‌آید.

از این به بعد، هر بار فقط همین `start.cmd` را بزن.

> پنجره مشکی را نبند تا وقتی داری کار می‌کنی. بستن آن یعنی خاموش کردن ابزار.

---

## قدم چهارم: کلید رایگان

بخش فنی ابزار (سرعت سایت، تکنولوژی‌ها، دامنه) بدون کلید هم کار می‌کند. ولی بخش تحلیل کسب‌وکار به یک کلید رایگان نیاز دارد.

۱. در داشبورد، بالا سمت راست روی **کلید API** بزن.

۲. راهنمای قدم‌به‌قدم همان‌جا باز می‌شود. دنبالش کن.

خلاصه‌اش: می‌روی [aistudio.google.com/api-keys](https://aistudio.google.com/api-keys)، با اکانت گوگل وارد می‌شوی، دکمه Create API key را می‌زنی، کلید را کپی می‌کنی و در داشبورد می‌چسبانی.

کارت بانکی لازم نیست. کلید روی کامپیوتر خودت می‌ماند و جایی فرستاده نمی‌شود.

---

## استفاده

آدرس سایت را بنویس (مثلاً `menapark.com`) و دکمه تحلیل را بزن. اولین بار حدود یک دقیقه طول می‌کشد.

بعدش می‌توانی:

- بین تب‌ها بچرخی تا کسب‌وکار، شکاف‌های مارکتینگ، سلامت فنی و هویت دامنه را ببینی
- زبان را از بالا سمت راست عوض کنی، تحلیل هم به همان زبان نوشته می‌شود
- گزارش را با دکمه‌های کنار حکم نهایی دانلود کنی
- روی علامت‌های **؟** موس نگه داری تا بفهمی هر بخش چه می‌گوید

---

## مشکلات رایج

**می‌گوید Python is not installed**
یعنی تیک Add python.exe to PATH را نزدی. پایتون را دوباره نصب کن و این بار تیک را بزن.

**ویندوز اجازه اجرا نمی‌دهد**
More info و بعد Run anyway.

**نصب وسط کار قطع می‌شود**
معمولاً مشکل اینترنت است. دوباره `install.cmd` را بزن.

**داشبورد باز نمی‌شود**
مطمئن شو پنجره مشکی هنوز باز است، بعد در مرورگر برو به `http://localhost:8080`

---

ساخته شده توسط عرفان حسن‌زاده · [erfandigital.com](https://erfandigital.com)

اگر ازش استفاده کردی، نظرت را بگو. چه مثبت چه منفی.
